import React from "react";
import {
  TradersList,
  Button,
  Chart,
} from './index'

function MainContainer(props) {
  
  return (
    <div>
      <TradersList/>
      <Chart/>
      <Button/>
    </div>
  )
}

export default MainContainer
